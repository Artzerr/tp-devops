services:

  database:
    build: ./database
    container_name: postgresql
    environment:
      - POSTGRES_DB=db
      - POSTGRES_USER=usr
      - POSTGRES_PASSWORD=pwd
    volumes:
      - pg-data:/var/lib/postgresql/data
    networks:
      - app-network

  backend:
    build: ./simple-api/simpleapi/simpleapi
    container_name: simple-api
    depends_on:
      - database
    networks:
      - app-network

  httpd:
    build: ./http-server
    container_name: my-http-server
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  pg-data: